/**
 * This is a utility function that leverages knex to dynamically fetch a value from a given column(s) in a given table.
 * @param {String} table 
 * @param {[String]} column 
 */
const fetchValue = (table, column) => {
    // SS TO-DO
}


const formatValue = (value) => {
    // SS TO-DO
}
